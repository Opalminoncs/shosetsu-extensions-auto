# Extension Icons

This directory contains icon images for extensions.

## Requirements

- Format: PNG or JPEG
- Recommended size: 256x256 pixels
- Named to match the extension ID or site name

Icons are referenced in `index.json` via the `imageURL` field for each extension.
