# Source Extensions

This directory contains the Lua source files for Shosetsu extensions.

## Directory Structure

- `en/` - English language sources
- `multi/` - Multi-language sources

Each `.lua` file implements the Shosetsu extension API and is listed in the root `index.json` manifest.
